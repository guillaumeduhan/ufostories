---
const {
  size = "h-12", // default Tailwind height class
  alt = "AstroDirectory.dev",
  class: className = "",
} = Astro.props;
---

<img 
  src="/logo.jpg" 
  alt={alt} 
  class={`${size} w-auto ${className}`} 
  loading="lazy" 
/>